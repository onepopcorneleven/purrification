# CLAUDE.md

Claude Code reads this file automatically at the start of every session in this
repository. Uncomment and fill in whichever sections are relevant to this
project, and delete the rest — an empty file is fine too if you don't need any
project-specific instructions yet.

<!--
## Project overview
Brief description of what this project does and how it's structured, e.g.:
This is a Node.js REST API for [x], using Express and PostgreSQL. Main entry
point is src/index.js. Routes live in src/routes/, business logic in src/services/.
-->

<!--
## Build & test commands
Commands Claude should run to verify its own work, e.g.:
- Install deps: npm install
- Run tests: npm test
- Lint: npm run lint
- Build: npm run build
Always run tests before committing.
-->

<!--
## Coding conventions
- Use conventional commits (feat:, fix:, chore:, docs:, refactor:)
- Follow existing code style (see .eslintrc / .prettierrc / pyproject.toml)
- Write or update tests for any new logic
-->

<!--
## Git & GitHub workflow rules
- Never commit directly to main — always branch first (git checkout -b feature/x)
- Never force-push
- Open a PR with `gh pr create` when work is ready for review; don't merge it yourself
- Keep commit messages focused — one logical change per commit
-->

<!--
## Directories or files to leave alone
- Do not modify anything under /legacy
- Do not edit generated files in /dist or /build
- Do not read or touch .env or anything listed in .gitignore
-->

<!--
## Deployment notes
Anything Claude should know before suggesting or running deploy-related steps, e.g.:
Deploys run via GitHub Actions on merge to main. Don't trigger deploys manually,
and don't edit the CI workflow files without flagging it first.
-->
