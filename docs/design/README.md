# `docs/design/`

Design-process artifacts for `docs/workplan.md` Phase 10 — style-tile
exports, palette/type decision notes, and similar working material
produced while executing the design plan. Kept separate from
`docs/design-system.md`, which is the narrative "how the UI will look"
doc itself, not a scratch space for the process that got there.
